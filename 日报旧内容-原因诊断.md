# 🐙 诊断报告：每日 HTML 日报仍是旧内容

> 排查时间：2026-08-01 · 排查对象：`k-macao/02` 的 `output/daily_report_*.html` / `latest.html`

## 一、结论（三个原因叠加）

每日日报“看起来是旧内容”不是推送或定时任务的问题（GitHub Actions 每天都在正常跑、正常提交），而是**报告内容本身大部分是写死的**，加上**实时数据源经常抓取失败落入写死的兜底数据**，以及**上一轮防覆盖保护逻辑不完整**。

---

## 二、原因 1（最主要）：报告主体是硬编码静态文本

`output/pipeline.py` 的 `generate_report()` 把以下内容全部写死在代码里，**不经过任何数据源**：

| 区块 | 写死的数值 |
|---|---|
| ⚡ 行情速览 | 道琼斯 51,618 -2.19%、标普 7,317 -1.52%、纳指 24,460 -1.74%、30年期国债 5.24%、WTI $84.9 +7.2%、核心PCE 3.3%、微软 +11%、Meta -9% |
| 🇨🇳 A股市场 | 上证 3,813 +0.40%、深成指 +1.10%、创业板 +1.55%、科创50 -0.87%、成交额 2.31万亿元 |
| 🎯 今日关注 | 财报/经济数据/地缘/央行/技术关口 五行全部写死 |
| 其他 | 热门话题 12 个标签、“市场共识/散户情绪/操作建议”批注全部写死 |

**证据：**
- `git show bc9a926^:output/pipeline.py`、`git show fe3f3e8:output/pipeline.py`、当前 `output/pipeline.py` 三份代码里都有完全相同的 `51,618 -2.19%`、`7,317`、`3,813`、`2.31万亿元` 硬编码 → 从 07-30 第一版日报起就没有实时行情。
- `daily_report_20260731.html` 与 `daily_report_20260801.html` 去掉时间戳后**相似度 97.7%**（19.4KB 里只有约 450 字节不同）。
- 08-01 当天两次运行（自动 10:57 vs 手动 13:08）的日报去掉时间戳后**相似度 98.1%**，唯一差异是新浪 5 条头条换了，其余（行情、WSB、韩股、Yahoo、话题、批注）逐字节相同。

> 结论：即使所有数据源都抓取成功，日报的“主体内容”也永远不会变，每天都会显示同一批旧数字。

---

## 三、原因 2：实时数据源在 GitHub Actions 上经常失败 → 落入写死的兜底数据

四个数据源里，**Reddit（www.reddit.com）与 Naver（api.stock.naver.com）在 Actions 机房 IP 下基本必失败**（反爬/屏蔽），Yahoo、新浪时好时坏。抓取失败时代码使用硬编码兜底列表，每天一模一样。

**证据（08-01 日报内直接可见）：**
- 🐂 Reddit WSB Top 10 恰好是兜底序列：NVDA **10** 次、AAPL **9** 次、MSFT **8** 次、META **7** 次、AMZN **6** 次、TSLA **5** 次、GOOGL **4** 次、AMD **3** 次、GME **2** 次、AMC **1** 次 —— 与 `fetch_wsb()` 兜底逻辑 `default_symbols` + `max(1, 10-i)` 完全一致。
- 🔌 韩股区 5 条与 07-31 日报完全相同，且逐字等于 `fetch_kospi_headlines()` 的兜底文案（“KOSPI 三连跌，外资持续流出”“三星电子 Q2 利润同比暴增 1814%”…）。
- 本环境实测：4 个源全部 `SSLZeroReturnError` 连接失败 → 全部走兜底（与 Actions 上的失败模式一致，只是 Actions 上 Yahoo/新浪偶尔能通）。
- 即 08-01 的日报 = Yahoo✅ 真实 + 新浪✅ 真实 + Reddit❌ 兜底 + Naver❌ 兜底，是一份“真假混合”的日报，但读者无法分辨。

> 结论：失败的源每天输出同一段旧文案，且报告里没有任何“此区块为兜底数据”的标注，看起来就是“旧内容”。

---

## 四、原因 3：上一轮“安全保护”（PR #6）逻辑不完整

`b558c35`（PR #6，08-01 凌晨合并）给 `save_report()` 加了防覆盖保护，但只覆盖了“最坏情况”：

1. **只有 4 个源全部失败时才保护**。像 08-01 这样 Yahoo/新浪成功、Reddit/Naver 失败（`is_all_fallback=False`），照常把“旧兜底+新数据”混合文件写盘覆盖。
2. **当天首次运行时保护不生效**：保护要求目标文件已存在（`os.path.isfile(output_path)`），而每天第一次生成时文件还不存在 → 全兜底内容会直接写成当天的“日报”。
3. **保护生效时反而故意保留旧文件**（“本次生成将不覆盖已有文件”），还把旧文件内容写进 `latest.html` —— 用户看到的就是“还是旧内容”。
4. 判定标记脆弱：用雅虎兜底首条 `"US stocks mixed as investors"` 是否存在于旧文件来猜旧文件“是否真实”，与内容真实性无必然关系。

> 结论：该保护既拦不住“部分兜底”的混合日报，也无法阻止全兜底日报在每天首次运行时产生；能拦住时又让旧内容持续可见。

---

## 五、次要原因

- 日报头部/页脚只标注**生成时间**（2026-08-01 13:08:10），没有任何区块的数据抓取时间或“抓取失败/兜底”标记 → 旧内容被包装成“新鲜出炉”。
- 07-31 的 `bc9a926` 大改版（删 461 行）还把 07-30 版里多出的“Reddit 全域股票”等数据源删掉了，日报信息量进一步缩水。

---

## 六、建议修复方向（供下一步决策）

1. **行情速览 / A股指数 / 今日关注改为实时抓取**（Yahoo quote API、新浪行情接口等），抓不到就显示“⚠️ 数据暂缺”而不是旧的写死数字。
2. **逐数据源标注**：每个区块显示来源+抓取时间；失败时明示“⚠️ xx 源抓取失败，以下为兜底数据”。
3. **重写 save_report 保护逻辑**：改为逐源合并/逐源保护，而不是整文件丢弃；首次运行若全兜底则拒绝生成“伪日报”（或生成但显著标注）。
4. 可选：为 Reddit/Naver 换用可用替代源（如 RSS 镜像、redlib 实例、其他行情 API），提高真实抓取成功率。

---

*证据文件：`output/daily_report_20260730.html`、`output/daily_report_20260731.html`、`output/daily_report_20260801.html`、`output/pipeline.py`、git 历史 `bc9a926`/`b558c35`/`fe3f3e8`。*
